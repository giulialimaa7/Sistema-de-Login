import { styled } from "styled-components";

export const HeadlineContainer = styled.div`
  max-width: 35rem;

  h1 {
    font-size: 3.6rem;
    line-height: 5.5rem;
    color: #1e293b;
  }

  h1 + p {
    color: #475569;
    margin-top: 1.6rem;
  }

`